; Kept as a reference only: this is a pnpm-specific setting.
; It is commented out because this project uses npm via `composer run dev`,
; and npm warns about unknown `public-hoist-pattern` keys.
; public-hoist-pattern[]=@inertiajs/core
